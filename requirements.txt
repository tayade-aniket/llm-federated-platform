# Core ML - Minimal memory footprint
torch==2.1.0 --extra-index-url https://download.pytorch.org/whl/cpu  # CPU version (smaller)
transformers==4.36.0
peft==0.7.0
datasets==2.15.0

# Federated Learning
flwr==1.5.0

# Web Interface
fastapi==0.104.0
uvicorn==0.24.0
jinja2==3.1.2

# Quantization (for low memory)
bitsandbytes==0.41.0  # CPU-only compatible

# Utilities
pyyaml==6.0.1
python-dotenv==1.0.0
tqdm==4.66.0